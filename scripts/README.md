# ⚡ Instant Commands

### **For Linux (Bash)**
```bash
curl -s <raw-url> | bash
```

### **Bash Alias (.bashrc/.zshrc)**
```
alias <name>='<command>'
```

### **For Windows (PowerShell)**
```
irm <raw-url> | iex
```
